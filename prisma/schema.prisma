// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
    provider = "prisma-client-js"
    output   = "../generated/prisma"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

enum UserPreset {
    HIGHSCHOOL
    COLLEGE
    UNIVERSITY
}

model Post {
    id        Int      @id @default(autoincrement())
    name      String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    createdBy   User   @relation(fields: [createdById], references: [id])
    createdById String

    @@index([name])
}

model Account {
    id                       String  @id @default(cuid())
    userId                   String
    type                     String
    provider                 String
    providerAccountId        String
    refresh_token            String?
    access_token             String?
    expires_at               Int?
    token_type               String?
    scope                    String?
    id_token                 String?
    session_state            String?
    user                     User    @relation(fields: [userId], references: [id], onDelete: Cascade)
    refresh_token_expires_in Int?

    @@unique([provider, providerAccountId])
}

model Session {
    id           String   @id @default(cuid())
    sessionToken String   @unique
    userId       String
    expires      DateTime
    user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model User {
    id            String    @id @default(cuid())
    name          String?
    email         String?   @unique
    emailVerified DateTime?
    image         String?
    password      String?
    learningStyle String?   @default("reading")
    autoAdapt     Boolean   @default(false)
    // Customization fields
    theme         String?   @default("light")
    accentColor   String?   @default("blue")
    fontSize      String?   @default("medium")
    compactMode   Boolean   @default(false)
    defaultNoteFormat String? @default("summary")
    autoSaveNotes Boolean   @default(true)
    emailNotifications Boolean @default(true)
    lastActive    DateTime?
    studyStreak   Int       @default(0)
    battleXp      Int       @default(0)
    battleWinStreak Int     @default(0)
    soloSessionsCompleted Int @default(0)
    battleAchievements String[] @default([])
    preset        UserPreset  @default(HIGHSCHOOL)
    presetSet     Boolean     @default(false)
    // Relations
    accounts      Account[]
    sessions      Session[]
    posts         Post[]
    notes         Note[]
    folders       Folder[]
    citations     Citation[]
    exams         Exam[]
    scanHistories ScanHistory[]
    examPredictions ExamPrediction[]
    hostedDuels Battle[] @relation("BattleHost")
    joinedBattles Battle[] @relation("BattleOpponent")
    battleParticipants BattleParticipant[]
    battleResults BattleResult[]
    createdStudyGroups StudyGroup[] @relation("StudyGroupCreator")
    studyGroupMemberships StudyGroupMember[]
    groupMessages GroupMessage[]
    sharedGroupNotes GroupSharedNote[] @relation("SharedNoteOwner")
    groupNoteComments GroupSharedNoteComment[]
    createdGroupFlashcards GroupFlashcard[]
    groupQuizRoundsStarted GroupQuizRound[] @relation("QuizRoundStarter")
    groupQuizSubmissions GroupQuizSubmission[]
    groupResourcesShared GroupResource[] @relation("ResourceSharer")
    groupSchedulesCreated GroupScheduleItem[] @relation("ScheduleCreator")
    groupStats GroupMemberStats[]
    hostedPomodoroTimers GroupPomodoroTimer[] @relation("PomodoroHost")
    pinnedGroupMessages GroupPinnedMessage[]
    conceptConnections ConceptConnection[]
    learningStyleResults LearningStyleResult[]
    conceptWebs ConceptWeb[]
    flashcardDecks FlashcardDeck[]
    novaStats NovaStats?
    hostedRooms StudyRoom[] @relation("RoomHost")
    roomMemberships RoomMember[]
    pdfDocuments PDFDocument[]
    annotations  Annotation[]
    focusSessions FocusSession[]
    feynmanSessions FeynmanSession[]
    studyPlans   StudyPlan[]
    calendarEvents CalendarEvent[]
    podcasts     Podcast[]
    diagrams     Diagram[]
    curriculumProgress OntarioCurriculumProgress[]
    wellnessEntries      WellnessEntry[]
    confidenceRatings    ConfidenceRating[]
    narrativeMemories    NarrativeMemory[]
    debateSessions       DebateSession[]
    interleavingSessions InterleavingSession[]
    communityPosts    CommunityPost[]
    communityLikes    CommunityLike[]
    communityComments CommunityComment[]
    habits            Habit[]
    habitEntries      HabitEntry[]
    quickCaptures     QuickCapture[]
    mockExamAttempts  MockExamAttempt[]
    mockExams         MockExam[]
    sharedDecks          SharedDeck[]
    teacherClasses       StudyClass[]          @relation("TeacherClasses")
    classMembers         StudyClassMember[]
    studyDNA             StudyDNA?
    examAutopsies        ExamAutopsy[]
    decayAlerts          DecayAlert[]
    conceptCollisions    ConceptCollision[]
    hostedBattles        BattleRoyale[]        @relation("HostedBattles")
    battlePlayers        BattleRoyalePlayer[]
    syllabusAnalyses     SyllabusAnalysis[]
    youtubeImports       YouTubeImport[]
    gradeCalculations    GradeCalculation[]
    studyContracts       StudyContract[]
    contractCheckIns     ContractCheckIn[]
    readingSessions      ReadingSession[]
    microLessons         MicroLesson[]
    adaptiveNotes        AdaptiveNote[]
    counterarguments     CounterargumentSession[]
    focusScores          FocusScore[]
    reviewsGiven         PeerReview[] @relation("ReviewsGiven")
    reviewsReceived      PeerReview[] @relation("ReviewsReceived")
    careerPath           CareerPath?
    lectureSessions      LectureSession[]
    crossoverChallenges  CrossoverChallenge[]
    memorySimulations    MemorySimulation[]
    kyvexIQ              KyvexIQ?
    noteEvolutions       NoteEvolution[]
    debatesAsPlayer1     DebateJudge[] @relation("DebatePlayer1")
    debatesAsPlayer2     DebateJudge[] @relation("DebatePlayer2")
    sip                  StudentIntelligenceProfile?
    autoTransformQueue   AutoTransformQueue[]
    smartSuggestions     SmartSuggestion[]
    studyModeSessions    StudyModeSession[]
    contentConnections   ContentConnection[]
    featurePreference    FeaturePreference?
}

model OntarioCurriculumCourse {
    id          String   @id @default(cuid())
    code        String   @unique
    title       String
    grade       Int
    subject     String
    category    String   @default("General")
    type        String   @default("Open")
    destination String
    description String   @db.Text
    keywords    String[] @default([])
    isSeeded    Boolean  @default(false)
    seededAt    DateTime?
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt

    units       OntarioCurriculumUnit[]
    progress    OntarioCurriculumProgress[]

    @@index([grade])
    @@index([subject])
    @@index([destination])
}

model OntarioCurriculumUnit {
    id          String   @id @default(cuid())
    courseId    String
    code        String
    title       String
    description String   @db.Text
    weight      Int      @default(0)
    orderIndex  Int      @default(0)
    createdAt   DateTime @default(now())

    course       OntarioCurriculumCourse @relation(fields: [courseId], references: [id], onDelete: Cascade)
    expectations OntarioCurriculumExpectation[]

    @@unique([courseId, code])
    @@index([courseId])
    @@index([orderIndex])
}

model OntarioCurriculumExpectation {
    id          String   @id @default(cuid())
    unitId      String
    code        String
    title       String
    description String   @db.Text
    strand      String
    createdAt   DateTime @default(now())

    unit OntarioCurriculumUnit @relation(fields: [unitId], references: [id], onDelete: Cascade)

    @@unique([unitId, code])
    @@index([unitId])
    @@index([strand])
}

model OntarioCurriculumProgress {
    id               String   @id @default(cuid())
    userId           String
    courseId         String
    completedUnits   String[] @default([])
    completedExpectations String[] @default([])
    confidence       Int      @default(0)
    lastStudiedAt    DateTime?
    createdAt        DateTime @default(now())
    updatedAt        DateTime @updatedAt

    user   User                   @relation(fields: [userId], references: [id], onDelete: Cascade)
    course OntarioCurriculumCourse @relation(fields: [courseId], references: [id], onDelete: Cascade)

    @@unique([userId, courseId])
    @@index([userId])
    @@index([courseId])
}

model VerificationToken {
    identifier String
    token      String   @unique
    expires    DateTime

    @@unique([identifier, token])
}

model Note {
  id        String   @id @default(cuid())
  title     String
  content   String   @db.Text
  format    String
    tags      String[] @default([])
    isPinned  Boolean  @default(false)
    lastViewedAt DateTime?
    isShared  Boolean  @default(false)
    folderId  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    folder    Folder?  @relation(fields: [folderId], references: [id], onDelete: SetNull)
        sharedInGroups GroupSharedNote[]
        adaptiveNote AdaptiveNote?
        evolutions   NoteEvolution[]

  @@index([createdAt])
  @@index([userId])
    @@index([folderId])
}

model FeynmanSession {
    id            String   @id @default(cuid())
    userId        String
    concept       String
    explanation   String   @db.Text
    score         Int
    gradeLabel    String
    feedback      Json
    attemptNumber Int      @default(1)
    createdAt     DateTime @default(now())

    user User @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([createdAt])
}

model StudyPlan {
    id        String   @id @default(cuid())
    userId    String
    weekStart DateTime
    subjects  String[]
    plan      Json
    createdAt DateTime @default(now())

    user User @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([weekStart])
    @@index([createdAt])
}

model CalendarEvent {
    id          String   @id @default(cuid())
    userId      String
    title       String
    date        DateTime
    endDate     DateTime?
    type        String   @default("deadline")
    color       String   @default("#f97316")
    description String?
    completed   Boolean  @default(false)
    createdAt   DateTime @default(now())

    user User @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([date])
    @@index([createdAt])
}

model Podcast {
    id         String   @id @default(cuid())
    userId     String
    user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    title      String
    sourceText String   @db.Text
    script     Json
    topic      String
    createdAt  DateTime @default(now())

    @@index([userId])
    @@index([createdAt])
}

model Diagram {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    title       String
    type        String
    sourceText  String   @db.Text
    diagramData Json
    createdAt   DateTime @default(now())

    @@index([userId])
    @@index([createdAt])
}

model Folder {
    id        String   @id @default(cuid())
    name      String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    userId    String
    user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    notes     Note[]

    @@index([userId])
}

model Citation {
    id          String   @id @default(cuid())
    author      String
    title       String
    publication String?
    date        String?
    url         String?
    pages       String?
    format      String
    createdAt   DateTime @default(now())

    userId String
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([createdAt])
}

model ExamPrediction {
    id              String   @id @default(cuid())
    examType        String
    uploadedContent String   @db.Text
    syllabusContent String?  @db.Text
    predictions     Json
    analytics       Json?
    // Smart forecast fields
    subject         String?
    examDate        DateTime?
    predictedScore  Float?
    confidence      Float?
    factors         Json?
    createdAt       DateTime @default(now())

    userId String
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([createdAt])
}

model Battle {
    id             String   @id @default(cuid())
    code           String   @unique
    noteId         String?
    title          String?
    status         String   @default("waiting")
    questionCount  Int      @default(10)
    mode           String   @default("pvp")
    subject        String?
    metadata       Json?
    questions      Json
    hostScore      Int      @default(0)
    opponentScore  Int      @default(0)
    hostAnsweredAt DateTime?
    opponentAnsweredAt DateTime?
    startedAt      DateTime?
    completedAt    DateTime?
    createdAt      DateTime @default(now())

    hostId     String
    opponentId String?

    host     User  @relation("BattleHost", fields: [hostId], references: [id], onDelete: Cascade)
    opponent User? @relation("BattleOpponent", fields: [opponentId], references: [id], onDelete: SetNull)

    battleQuestions BattleQuestion[]
    participants BattleParticipant[]
    result       BattleResult?

    @@index([hostId])
    @@index([opponentId])
    @@index([status])
    @@index([createdAt])
}

model BattleQuestion {
    id            String  @id @default(cuid())
    battleId      String
    question      String  @db.Text
    options       Json?
    correctAnswer String
    points        Int     @default(10)
    orderIndex    Int

    battle Battle @relation(fields: [battleId], references: [id], onDelete: Cascade)

    @@index([battleId])
}

model BattleParticipant {
    id             String   @id @default(cuid())
    battleId       String
    userId         String
    answers        Json?
    score          Int      @default(0)
    correctCount   Int      @default(0)
    totalAnswered  Int      @default(0)
    joinedAt       DateTime @default(now())

    battle Battle @relation(fields: [battleId], references: [id], onDelete: Cascade)
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@unique([battleId, userId])
    @@index([userId])
}

model BattleResult {
    id        String   @id @default(cuid())
    battleId  String   @unique
    winnerId  String?
    scores    Json
    duration  Int
    createdAt DateTime @default(now())

    battle  Battle @relation(fields: [battleId], references: [id], onDelete: Cascade)
    winner  User?  @relation(fields: [winnerId], references: [id], onDelete: SetNull)

    @@index([winnerId])
}

model StudyGroup {
    id        String   @id @default(cuid())
    name      String
    topic     String?
    inviteCode String  @unique
    isPublic  Boolean  @default(false)
    streakCount Int    @default(0)
    lastActiveAt DateTime?
    weeklyResetAt DateTime?
    settings Json?
    createdAt DateTime @default(now())

    creatorId String
    creator   User   @relation("StudyGroupCreator", fields: [creatorId], references: [id], onDelete: Cascade)

    members  StudyGroupMember[]
    messages GroupMessage[]
    sessions GroupSession[]
    sharedNotes GroupSharedNote[]
    flashcards GroupFlashcard[]
    quizRounds GroupQuizRound[]
    scheduleItems GroupScheduleItem[]
    resources GroupResource[]
    leaderboardStats GroupMemberStats[]
    pomodoroTimer GroupPomodoroTimer?
    pinnedMessages GroupPinnedMessage[]

    @@index([creatorId])
}

model StudyGroupMember {
    id        String   @id @default(cuid())
    groupId   String
    userId    String
    role      String   @default("member")
    lastSeenAt DateTime?
    joinedAt  DateTime @default(now())

    group StudyGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)
    user  User       @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@unique([groupId, userId])
    @@index([userId])
}

model GroupMessage {
    id        String   @id @default(cuid())
    groupId   String
    userId    String?
    message   String   @db.Text
    isAI      Boolean  @default(false)
    metadata  Json?
    timestamp DateTime @default(now())

    group StudyGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)
    user  User?      @relation(fields: [userId], references: [id], onDelete: SetNull)
    pinnedInGroups GroupPinnedMessage[]

    @@index([groupId])
    @@index([timestamp])
}

model GroupSession {
    id        String   @id @default(cuid())
    groupId   String
    summary   String   @db.Text
    duration  Int
    topics    Json?
    createdAt DateTime @default(now())

    group StudyGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)

    @@index([groupId])
}

model GroupSharedNote {
    id         String   @id @default(cuid())
    groupId    String
    noteId     String
    sharedById String
    pinned     Boolean  @default(false)
    createdAt  DateTime @default(now())

    group    StudyGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)
    note     Note       @relation(fields: [noteId], references: [id], onDelete: Cascade)
    sharedBy User       @relation("SharedNoteOwner", fields: [sharedById], references: [id], onDelete: Cascade)
    comments GroupSharedNoteComment[]

    @@unique([groupId, noteId])
    @@index([groupId])
    @@index([sharedById])
}

model GroupSharedNoteComment {
    id         String   @id @default(cuid())
    sharedNoteId String
    userId     String
    comment    String   @db.Text
    createdAt  DateTime @default(now())

    sharedNote GroupSharedNote @relation(fields: [sharedNoteId], references: [id], onDelete: Cascade)
    user       User            @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([sharedNoteId])
    @@index([userId])
}

model GroupFlashcard {
    id         String   @id @default(cuid())
    groupId    String
    creatorId  String
    front      String   @db.Text
    back       String   @db.Text
    knownBy    String[] @default([])
    learningBy String[] @default([])
    createdAt  DateTime @default(now())

    group   StudyGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)
    creator User       @relation(fields: [creatorId], references: [id], onDelete: Cascade)

    @@index([groupId])
    @@index([creatorId])
}

model GroupQuizRound {
    id                 String   @id @default(cuid())
    groupId            String
    startedById        String
    title              String
    status             String   @default("active")
    questions          Json
    currentQuestionIdx Int      @default(0)
    startedAt          DateTime @default(now())
    completedAt        DateTime?

    group      StudyGroup           @relation(fields: [groupId], references: [id], onDelete: Cascade)
    startedBy  User                 @relation("QuizRoundStarter", fields: [startedById], references: [id], onDelete: Cascade)
    submissions GroupQuizSubmission[]

    @@index([groupId])
    @@index([status])
}

model GroupQuizSubmission {
    id         String   @id @default(cuid())
    roundId    String
    userId     String
    answers    Json?
    score      Int      @default(0)
    completedAt DateTime?
    updatedAt  DateTime @updatedAt

    round GroupQuizRound @relation(fields: [roundId], references: [id], onDelete: Cascade)
    user  User           @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@unique([roundId, userId])
    @@index([userId])
}

model GroupScheduleItem {
    id         String   @id @default(cuid())
    groupId    String
    createdById String
    title      String
    startsAt   DateTime
    endsAt     DateTime?
    createdAt  DateTime @default(now())

    group     StudyGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)
    createdBy User       @relation("ScheduleCreator", fields: [createdById], references: [id], onDelete: Cascade)

    @@index([groupId])
    @@index([startsAt])
}

model GroupResource {
    id          String   @id @default(cuid())
    groupId     String
    sharedById  String
    type        String
    title       String
    url         String?
    noteId      String?
    metadata    Json?
    pinned      Boolean  @default(false)
    createdAt   DateTime @default(now())

    group    StudyGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)
    sharedBy User       @relation("ResourceSharer", fields: [sharedById], references: [id], onDelete: Cascade)

    @@index([groupId])
    @@index([sharedById])
    @@index([type])
}

model GroupMemberStats {
    id               String   @id @default(cuid())
    groupId          String
    userId           String
    weekKey          String
    notesSaved       Int      @default(0)
    quizzesCompleted Int      @default(0)
    messagesSent     Int      @default(0)
    weeklyScore      Int      @default(0)
    allTimeScore     Int      @default(0)
    updatedAt        DateTime @updatedAt

    group StudyGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)
    user  User       @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@unique([groupId, userId, weekKey])
    @@index([groupId])
    @@index([userId])
}

model GroupPomodoroTimer {
    id               String   @id @default(cuid())
    groupId          String   @unique
    hostId           String
    status           String   @default("paused")
    mode             String   @default("study")
    remainingSeconds Int      @default(1500)
    cycleCount       Int      @default(0)
    startedAt        DateTime?
    updatedAt        DateTime @updatedAt

    group StudyGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)
    host  User       @relation("PomodoroHost", fields: [hostId], references: [id], onDelete: Cascade)
}

model GroupPinnedMessage {
    id         String   @id @default(cuid())
    groupId    String
    messageId  String
    pinnedById String
    createdAt  DateTime @default(now())

    group    StudyGroup  @relation(fields: [groupId], references: [id], onDelete: Cascade)
    message  GroupMessage @relation(fields: [messageId], references: [id], onDelete: Cascade)
    pinnedBy User         @relation(fields: [pinnedById], references: [id], onDelete: Cascade)

    @@index([groupId])
    @@index([messageId])
}

model ConceptConnection {
    id                    String   @id @default(cuid())
    note1Id               String
    note2Id               String
    concept1              String
    concept2              String
    connectionDescription String   @db.Text
    strength              String
    createdAt             DateTime @default(now())

    userId String
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([createdAt])
}

model LearningStyleResult {
    id                 String   @id @default(cuid())
    userId             String
    dominantStyle      String
    visualPercent      Int
    auditoryPercent    Int
    readingPercent     Int
    kinestheticPercent Int
    createdAt          DateTime @default(now())

    user User @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([createdAt])
    @@index([dominantStyle])
}

model ConceptWeb {
    id         String   @id @default(cuid())
    userId     String
    title      String
    topic      String
    webData    Json
    shareToken String   @unique
    isShared   Boolean  @default(false)
    createdAt  DateTime @default(now())
    updatedAt  DateTime @updatedAt

    user User @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([topic])
    @@index([createdAt])
}

model Exam {
    id               String    @id @default(cuid())
    userId           String
    subject          String
    examDate         DateTime
    board            String?
    difficulty       String?
    topics           String?
    studyPlan        String?
    resultRecorded   Boolean   @default(false)
    scorePercent     Float?
    gradeKU          Float?
    gradeThinking    Float?
    gradeComm        Float?
    gradeApp         Float?
    resultNotes      String?
    resultRecordedAt DateTime?
    createdAt        DateTime  @default(now())

    user User @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([examDate])
    @@index([createdAt])
}

model ScanHistory {
    id         String   @id @default(cuid())
    userId     String
    confidence Int
    wordCount  Int
    subject    String?
    createdAt  DateTime @default(now())

    user User @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([createdAt])
}

model FlashcardDeck {
    id          String      @id @default(cuid())
    userId      String
    title       String
    subject     String
    description String?
    createdAt   DateTime    @default(now())
    updatedAt   DateTime    @updatedAt

    user  User        @relation(fields: [userId], references: [id], onDelete: Cascade)
    cards Flashcard[]
    sharedDecks SharedDeck[]

    @@index([userId])
    @@index([createdAt])
}

model Flashcard {
    id           String        @id @default(cuid())
    deckId       String
    front        String
    back         String
    easeFactor   Float         @default(2.5)
    interval     Int           @default(1)
    repetitions  Int           @default(0)
    nextReview   DateTime      @default(now())
    lastReviewed DateTime?
    createdAt    DateTime      @default(now())

    deck FlashcardDeck @relation(fields: [deckId], references: [id], onDelete: Cascade)
    confidenceRatings    ConfidenceRating[]

    @@index([deckId])
    @@index([nextReview])
    @@index([lastReviewed])
}

model NovaStats {
    id                String   @id @default(cuid())
    userId            String   @unique
    happiness         Int      @default(50)
    totalXP           Int      @default(0)
    level             Int      @default(1)
    streak            Int      @default(0)
    lastActiveDate    DateTime?
    notesGenerated    Int      @default(0)
    flashcardsStudied Int      @default(0)
    audioConverted    Int      @default(0)
    battlesWon        Int      @default(0)
    createdAt         DateTime @default(now())
    updatedAt         DateTime @updatedAt
    user              User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model StudyRoom {
    id            String      @id @default(cuid())
    name          String
    subject       String
    description   String?
    hostId        String
    isPublic      Boolean     @default(true)
    maxMembers    Int         @default(10)
    pomodoroState Json?
    createdAt     DateTime    @default(now())
    updatedAt     DateTime    @updatedAt
    members       RoomMember[]
    host          User        @relation("RoomHost", fields: [hostId], references: [id])

    @@index([hostId])
    @@index([createdAt])
}

model RoomMember {
    id       String    @id @default(cuid())
    roomId   String
    userId   String
    joinedAt DateTime  @default(now())
    lastSeen DateTime  @default(now())
    status   String    @default("studying")
    room     StudyRoom @relation(fields: [roomId], references: [id], onDelete: Cascade)
    user     User      @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@unique([roomId, userId])
    @@index([roomId])
    @@index([userId])
    @@index([lastSeen])
}

model PDFDocument {
    id          String       @id @default(cuid())
    userId      String
    title       String
    fileName    String
    fileSize    Int
    pageCount   Int
    blobUrl     String?
    createdAt   DateTime     @default(now())
    updatedAt   DateTime     @updatedAt
    user        User         @relation(fields: [userId], references: [id], onDelete: Cascade)
    annotations Annotation[]

    @@index([userId])
    @@index([updatedAt])
}

model Annotation {
    id        String      @id @default(cuid())
    docId     String
    userId    String
    page      Int
    type      String
    color     String
    text      String?
    note      String?
    rects     Json
    createdAt DateTime    @default(now())
    updatedAt DateTime    @updatedAt
    doc       PDFDocument @relation(fields: [docId], references: [id], onDelete: Cascade)
    user      User        @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([docId])
    @@index([userId])
    @@index([page])
}

model FocusSession {
    id           String   @id @default(cuid())
    userId       String
    goal         String?
    durationMins Int
    actualMins   Int?
    completed    Boolean  @default(false)
    abandoned    Boolean  @default(false)
    distractions Int      @default(0)
    startedAt    DateTime @default(now())
    endedAt      DateTime?
    user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@index([userId])
    @@index([startedAt])
}

model WellnessEntry {
    id           String   @id @default(cuid())
    userId       String
    user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    mood         Int
    energy       Int
    stress       Int
    notes        String?
    burnoutScore Float
    createdAt    DateTime @default(now())

    @@index([userId])
    @@index([createdAt])
}

model ConfidenceRating {
    id          String    @id @default(cuid())
    userId      String
    user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
    flashcardId String
    flashcard   Flashcard @relation(fields: [flashcardId], references: [id], onDelete: Cascade)
    rating      Int
    wasCorrect  Boolean
    createdAt   DateTime  @default(now())

    @@index([userId])
    @@index([flashcardId])
    @@index([createdAt])
}

model NarrativeMemory {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    noteId      String?
    sourceText  String   @db.Text
    narrative   String   @db.Text
    topic       String
    createdAt   DateTime @default(now())

    @@index([userId])
    @@index([topic])
    @@index([createdAt])
}

model DebateSession {
    id               String   @id @default(cuid())
    userId           String
    user             User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    topic            String
    forArguments     Json
    againstArguments Json
    verdict          String?
    createdAt        DateTime @default(now())

    @@index([userId])
    @@index([createdAt])
}

model InterleavingSession {
    id           String   @id @default(cuid())
    userId       String
    user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    subjects     String[]
    schedule     Json
    totalMinutes Int
    createdAt    DateTime @default(now())

    @@index([userId])
    @@index([createdAt])
}

model CommunityPost {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    content     String   @db.Text
    topic       String?
    likes       CommunityLike[]
    comments    CommunityComment[]
    createdAt   DateTime @default(now())
}

model CommunityLike {
    id        String   @id @default(cuid())
    userId    String
    user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    postId    String
    post      CommunityPost @relation(fields: [postId], references: [id], onDelete: Cascade)
    createdAt DateTime @default(now())

    @@unique([userId, postId])
}

model CommunityComment {
    id        String   @id @default(cuid())
    userId    String
    user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    postId    String
    post      CommunityPost @relation(fields: [postId], references: [id], onDelete: Cascade)
    content   String   @db.Text
    createdAt DateTime @default(now())
}

model HabitEntry {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    habitName   String
    completed   Boolean  @default(false)
    date        DateTime @default(now())
    createdAt   DateTime @default(now())
}

model Habit {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    name        String
    emoji       String   @default("✅")
    streak      Int      @default(0)
    bestStreak  Int      @default(0)
    color       String   @default("#f0b429")
    active      Boolean  @default(true)
    createdAt   DateTime @default(now())
}

model QuickCapture {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    content     String   @db.Text
    tags        String[]
    processed   Boolean  @default(false)
    convertedTo String?
    createdAt   DateTime @default(now())
}

model MockExam {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    title       String
    subject     String
    questions   MockExamQuestion[]
    attempts    MockExamAttempt[]
    timeLimit   Int
    createdAt   DateTime @default(now())
}

model MockExamQuestion {
    id          String   @id @default(cuid())
    examId      String
    exam        MockExam @relation(fields: [examId], references: [id], onDelete: Cascade)
    question    String   @db.Text
    options     Json
    answer      String
    explanation String   @db.Text
    type        String   @default("mcq")
    points      Int      @default(1)
}

model MockExamAttempt {
    id          String   @id @default(cuid())
    examId      String
    exam        MockExam @relation(fields: [examId], references: [id], onDelete: Cascade)
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    answers     Json
    score       Float
    timeTaken   Int
    createdAt   DateTime @default(now())
}

model SharedDeck {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    deckId      String
    deck        FlashcardDeck @relation(fields: [deckId], references: [id], onDelete: Cascade)
    title       String
    subject     String
    description String?
    preset      String   @default("HIGHSCHOOL")
    likes       Int      @default(0)
    downloads   Int      @default(0)
    createdAt   DateTime @default(now())
}

model StudyClass {
    id          String   @id @default(cuid())
    teacherId   String
    teacher     User     @relation("TeacherClasses", fields: [teacherId], references: [id], onDelete: Cascade)
    name        String
    subject     String
    code        String   @unique
    members     StudyClassMember[]
    assignments StudyClassAssignment[]
    createdAt   DateTime @default(now())
}

model StudyClassMember {
    id        String   @id @default(cuid())
    classId   String
    class     StudyClass @relation(fields: [classId], references: [id], onDelete: Cascade)
    userId    String
    user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    joinedAt  DateTime @default(now())

    @@unique([classId, userId])
}

model StudyClassAssignment {
    id          String   @id @default(cuid())
    classId     String
    class       StudyClass @relation(fields: [classId], references: [id], onDelete: Cascade)
    title       String
    description String?
    dueDate     DateTime?
    type        String
    resourceId  String?
    createdAt   DateTime @default(now())
}

model StudyDNA {
    id                String   @id @default(cuid())
    userId            String   @unique
    user              User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    visualScore       Float    @default(0)
    auditoryScore     Float    @default(0)
    readWriteScore    Float    @default(0)
    kinestheticScore  Float    @default(0)
    bestTimeOfDay     String   @default("morning")
    avgSessionMinutes Int      @default(25)
    strongSubjects    String[]
    weakSubjects      String[]
    learningVelocity  Float    @default(1.0)
    profile           Json
    updatedAt         DateTime @updatedAt
    createdAt         DateTime @default(now())
}

model ExamAutopsy {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    examId        String?
    subject       String
    score         Float
    totalMarks    Float
    diagnosis     Json
    weakAreas     String[]
    strongAreas   String[]
    actionPlan    Json
    createdAt     DateTime @default(now())
}

model DecayAlert {
    id                  String   @id @default(cuid())
    userId              String
    user                User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    conceptId           String
    conceptType         String
    conceptTitle        String
    decayScore          Float
    predictedForgetDate DateTime
    lastReviewed        DateTime
    dismissed           Boolean  @default(false)
    createdAt           DateTime @default(now())
}

model ConceptCollision {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    concept1      String
    concept2      String
    subject1      String
    subject2      String
    connection    String   @db.Text
    strength      Float
    createdAt     DateTime @default(now())
}

model BattleRoyale {
    id            String   @id @default(cuid())
    hostId        String
    host          User     @relation("HostedBattles", fields: [hostId], references: [id], onDelete: Cascade)
    code          String   @unique
    subject       String
    status        String   @default("waiting")
    maxPlayers    Int      @default(100)
    players       BattleRoyalePlayer[]
    questions     Json
    currentQ      Int      @default(0)
    winnerId      String?
    createdAt     DateTime @default(now())
}

model BattleRoyalePlayer {
    id            String   @id @default(cuid())
    battleId      String
    battle        BattleRoyale @relation(fields: [battleId], references: [id], onDelete: Cascade)
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    score         Int      @default(0)
    eliminated    Boolean  @default(false)
    rank          Int?
    joinedAt      DateTime @default(now())

    @@unique([battleId, userId])
}

model SyllabusAnalysis {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    originalText  String   @db.Text
    courseName    String
    semester      String
    plan          Json
    events        Json
    createdAt     DateTime @default(now())
}

model YouTubeImport {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    youtubeUrl    String
    title         String
    transcript    String   @db.Text
    notes         String   @db.Text
    flashcards    Json
    createdAt     DateTime @default(now())
}

model GradeCalculation {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    courseName    String
    currentGrade  Float
    currentWeight Float
    finalWeight   Float
    targetGrade   Float
    neededOnFinal Float
    createdAt     DateTime @default(now())
}

model StudyContract {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    commitment    String   @db.Text
    dailyHours    Float
    durationDays  Int
    startDate     DateTime @default(now())
    endDate       DateTime
    status        String   @default("active")
    checkIns      ContractCheckIn[]
    currentStreak Int      @default(0)
    totalDays     Int      @default(0)
    createdAt     DateTime @default(now())
}

model ContractCheckIn {
    id            String        @id @default(cuid())
    contractId    String
    contract      StudyContract @relation(fields: [contractId], references: [id], onDelete: Cascade)
    userId        String
    user          User          @relation(fields: [userId], references: [id], onDelete: Cascade)
    hoursStudied  Float
    notes         String?
    aiResponse    String        @db.Text
    mood          Int           @default(3)
    createdAt     DateTime      @default(now())
}

model ReadingSession {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    text          String   @db.Text
    topic         String
    wpm           Int      @default(0)
    comprehension Float    @default(0)
    targetWpm     Int      @default(250)
    questions     Json
    answers       Json
    createdAt     DateTime @default(now())
}

model MicroLesson {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    topic         String
    subject       String
    lessons       Json
    totalLessons  Int
    completed     Int      @default(0)
    createdAt     DateTime @default(now())
}

model AdaptiveNote {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    noteId        String   @unique
    note          Note     @relation(fields: [noteId], references: [id], onDelete: Cascade)
    level1        String   @db.Text
    level2        String   @db.Text
    level3        String   @db.Text
    level4        String   @db.Text
    currentLevel  Int      @default(1)
    createdAt     DateTime @default(now())
}

model CounterargumentSession {
    id               String   @id @default(cuid())
    userId           String
    user             User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    originalArgument String   @db.Text
    topic            String
    counterarguments Json
    userRebuttals    Json
    score            Float    @default(0)
    createdAt        DateTime @default(now())
}

model FocusScore {
    id            String   @id @default(cuid())
    userId        String
    user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    sessionDate   DateTime @default(now())
    score         Float
    activeMinutes Int
    totalMinutes  Int
    distractions  Int      @default(0)
    deepWorkRatio Float
    createdAt     DateTime @default(now())
}

model PeerReview {
    id          String   @id @default(cuid())
    authorId    String
    author      User     @relation("ReviewsGiven", fields: [authorId], references: [id], onDelete: Cascade)
    reviewerId  String?
    reviewer    User?    @relation("ReviewsReceived", fields: [reviewerId], references: [id])
    content     String   @db.Text
    contentType String
    subject     String
    status      String   @default("pending")
    feedback    String?  @db.Text
    rating      Int?
    aiFeedback  String?  @db.Text
    createdAt   DateTime @default(now())
}

model CareerPath {
    id              String   @id @default(cuid())
    userId          String   @unique
    user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    strongSubjects  String[]
    interests       String[]
    paths           Json
    topPath         String
    requiredCourses Json
    updatedAt       DateTime @updatedAt
    createdAt       DateTime @default(now())
}

model LectureSession {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    title       String
    transcript  String   @db.Text
    notes       String   @db.Text
    flashcards  Json
    duration    Int
    subject     String
    createdAt   DateTime @default(now())
}

model CrossoverChallenge {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    subject1    String
    subject2    String
    challenge   String   @db.Text
    userAnswer  String?  @db.Text
    aiFeedback  String?  @db.Text
    score       Float?
    date        DateTime @default(now())
    completed   Boolean  @default(false)
    createdAt   DateTime @default(now())
}

model MemorySimulation {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    deckId      String
    simulation  Json
    generatedAt DateTime @default(now())
}

model KyvexIQ {
    id                String   @id @default(cuid())
    userId            String   @unique
    user              User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    score             Float    @default(0)
    masteryScore      Float    @default(0)
    consistencyScore  Float    @default(0)
    velocityScore     Float    @default(0)
    depthScore        Float    @default(0)
    breakdown         Json
    rank              String   @default("Beginner")
    updatedAt         DateTime @updatedAt
    createdAt         DateTime @default(now())
}

model NoteEvolution {
    id          String   @id @default(cuid())
    noteId      String
    note        Note     @relation(fields: [noteId], references: [id], onDelete: Cascade)
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    snapshot    String   @db.Text
    wordCount   Int
    version     Int
    createdAt   DateTime @default(now())
}

model DebateJudge {
    id          String   @id @default(cuid())
    topic       String
    player1Id   String
    player1     User     @relation("DebatePlayer1", fields: [player1Id], references: [id], onDelete: Cascade)
    player2Id   String?
    player2     User?    @relation("DebatePlayer2", fields: [player2Id], references: [id])
    player1Arg  String?  @db.Text
    player2Arg  String?  @db.Text
    verdict     String?  @db.Text
    winnerId    String?
    scores      Json?
    status      String   @default("waiting")
    code        String   @unique
    createdAt   DateTime @default(now())
}

model StudentIntelligenceProfile {
    id                String   @id @default(cuid())
    userId            String   @unique
    user              User     @relation(fields: [userId], references: [id], onDelete: Cascade)

    // KNOWLEDGE
    knowledgeBreadth  Float    @default(0)
    retentionScore    Float    @default(0)
    depthScore        Float    @default(0)
    performanceScore  Float    @default(0)
    consistencyScore  Float    @default(0)
    wellbeingScore    Float    @default(0)
    disciplineScore   Float    @default(0)
    synthesisScore    Float    @default(0)
    velocityScore     Float    @default(0)

    // PROFILE
    chronotype        String   @default("unknown")
    dominantStyle     String   @default("unknown")
    strongestSubjects String[]
    weakestSubjects   String[]
    subjectGraph      Json     @default("{}")
    featureUsage      Json     @default("{}")
    learningPatterns  Json     @default("{}")

    // PROGRESSION GATES
    flashcardGate     String   @default("standard")
    difficultyGate    String   @default("normal")
    contentUnlocks    String[]

    // SUGGESTIONS
    lastSuggestions   Json     @default("[]")
    suggestionHistory Json     @default("[]")

    // META
    totalStudyHours   Float    @default(0)
    lastCalculated    DateTime @default(now())
    updatedAt         DateTime @updatedAt
    createdAt         DateTime @default(now())
}

model AutoTransformQueue {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    sourceType  String
    sourceId    String
    sourceTitle String
    subject     String?
    status      String   @default("pending")
    results     Json     @default("{}")
    createdAt   DateTime @default(now())
    processedAt DateTime?
}

model SmartSuggestion {
    id          String   @id @default(cuid())
    userId      String
    user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    trigger     String
    suggestions Json
    shown       Boolean  @default(false)
    actedOn     Boolean  @default(false)
    createdAt   DateTime @default(now())
}

model StudyModeSession {
    id           String   @id @default(cuid())
    userId       String
    user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    topic        String
    subject      String
    totalMinutes Int
    phases       Json
    currentPhase Int      @default(0)
    status       String   @default("active")
    results      Json     @default("{}")
    startedAt    DateTime @default(now())
    completedAt  DateTime?
    createdAt    DateTime @default(now())
}

model ContentConnection {
    id         String   @id @default(cuid())
    userId     String
    user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    sourceType String
    sourceId   String
    targetType String
    targetId   String
    strength   Float    @default(0)
    reason     String?
    createdAt  DateTime @default(now())

    @@unique([sourceId, targetId])
}

model FeaturePreference {
    id              String   @id @default(cuid())
    userId          String   @unique
    user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)
    enabledFeatures String[]
    hiddenFeatures  String[]
    preset          String   @default("HIGHSCHOOL")
    customized      Boolean  @default(false)
    updatedAt       DateTime @updatedAt
    createdAt       DateTime @default(now())
}